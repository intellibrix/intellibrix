import { describe, expect, it } from 'vitest'
import { placeholder } from '../src/index.js'

describe('@intellibrix/mcp scaffold', () => {
  it('resolves', () => {
    expect(placeholder).toBe(true)
  })
})
