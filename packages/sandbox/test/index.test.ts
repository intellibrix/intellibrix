import { describe, expect, it } from 'vitest'
import { placeholder } from '../src/index.js'

describe('@intellibrix/sandbox scaffold', () => {
  it('resolves', () => {
    expect(placeholder).toBe(true)
  })
})
